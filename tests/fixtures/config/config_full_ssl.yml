influx_db:
    default_connection: default # No sense to specify it. This is it just for test coverage.
    host: localhost
    database: telegraf
    ssl: true
    udp_port: 1337
    http_port: 42
    username: 'foo'
    password: 'bar'
